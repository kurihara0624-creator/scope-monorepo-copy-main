# =============================================================================
# M O N O R E P O   -   I G N O R E   L I S T
# =============================================================================

# 1. 依存関係 (node_modules)
# -----------------------------------------------------------------------------
# 'npm install' で再生成されるため、Gitの管理対象から除外します。
# この一行で、プロジェクト内の全てのnode_modulesが対象になります。
node_modules/


# 2. ビルド成果物
# -----------------------------------------------------------------------------
# 'npm run build' で再生成される、公開用のファイル群です。
dist/
dist-ssr/


# 3. 環境変数ファイル
# -----------------------------------------------------------------------------
# APIキーなどの機密情報が含まれるため、絶対にGitに含めてはいけません。
# 代わりに'.env.example'ファイルをコミットするのがベストプラクティスです。
.env
.env.*.local
*.env


# 4. ログファイル
# -----------------------------------------------------------------------------
# 実行時に生成される一時的なログファイルです。
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*


# 5. OS / エディタ固有のファイル
# -----------------------------------------------------------------------------
# 開発者個人の環境に依存する、共有不要なファイルです。

# macOS
.DS_Store

# VSCode
.vscode/*
!.vscode/settings.json
!.vscode/extensions.json

# JetBrains (WebStorm, etc.)
.idea/

# Visual Studio
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?


# 6. Vite / Turbopack 固有のキャッシュ
# -----------------------------------------------------------------------------
# 開発サーバーが高速化のために使用する一時ファイルです。
.vite/
.turbo/


# package-lock.jsonを無視する
package-lock.json
.nvmrc
# Local Netlify folder
.netlify
